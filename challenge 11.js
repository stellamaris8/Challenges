// function calcularAreaTriangulo(base, altura) {
//     return`El área del triángulo es ${(base * altura) /2}`;
// }

// console.log (calcularAreaTriangulo(40,30));


function calcularAreaCirculo ( PIxr2) {
    
    return`El área del círculo es ${(Math.PI * Math.pow(20,2)).toFixed(2)}`;
}

console.log (calcularAreaCirculo (Math.PI * Math.pow(20,2))) ;